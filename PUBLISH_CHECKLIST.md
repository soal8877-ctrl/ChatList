# Чеклист публикации на GitHub

## ✅ Перед первым релизом

- [ ] Заменить `yourusername` на ваш GitHub username в:
  - [ ] `docs/index.html` (3 места)
  - [ ] `CHANGELOG.md` (ссылки внизу)
  - [ ] `README.md` (если есть ссылки)
  
- [ ] Проверить, что все файлы закоммичены:
  ```bash
  git status
  ```

- [ ] Закоммитить все изменения:
  ```bash
  git add .
  git commit -m "Add GitHub Pages and release automation"
  git push origin main
  ```

- [ ] Включить GitHub Pages:
  - Settings → Pages
  - Source: `main` / `docs`
  - Save

- [ ] Проверить, что сайт доступен:
  - `https://yourusername.github.io/ChatList/`

## ✅ Перед каждым релизом

- [ ] Обновить версию:
  ```bash
  python update_version.py 1.0.1
  ```
  Или вручную в `version.py`

- [ ] Обновить CHANGELOG.md с описанием изменений

- [ ] Протестировать сборку локально:
  ```bash
  python build.py
  python build_installer.py  # если установлен Inno Setup
  ```

- [ ] Закоммитить изменения:
  ```bash
  git add .
  git commit -m "Bump version to 1.0.1"
  git push origin main
  ```

- [ ] Создать и запушить тег:
  ```bash
  git tag -a v1.0.1 -m "Release version 1.0.1"
  git push origin v1.0.1
  ```

- [ ] Проверить, что GitHub Actions запустился:
  - Actions → Проверить статус workflow

- [ ] Проверить релиз:
  - Releases → Убедиться, что файлы загружены
  - Проверить описание релиза

- [ ] Обновить сайт (если нужно):
  - Обновить информацию в `docs/index.html`
  - Закоммитить и запушить изменения

## 📝 Полезные команды

### Обновление версии во всех файлах
```bash
python update_version.py 1.0.1
```

### Создание тега
```bash
git tag -a v1.0.1 -m "Release version 1.0.1"
git push origin v1.0.1
```

### Просмотр всех тегов
```bash
git tag -l
```

### Удаление тега (если ошибка)
```bash
git tag -d v1.0.1
git push origin :refs/tags/v1.0.1
```

### Локальная проверка сборки
```bash
python build.py
python build_installer.py
```

## 🔗 Полезные ссылки

- [Подробная инструкция](GITHUB_PUBLISH.md)
- [Быстрый старт](QUICK_START.md)
- [Шаблон Release Notes](RELEASE_NOTES_TEMPLATE.md)
- [Changelog](CHANGELOG.md)
